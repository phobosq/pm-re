name: PM62C NVIDIA NVAPI table

on:
  push:
    paths:
      - '.github/workflows/run-nvapi-table.yml'
      - 'scripts/decode_nvapi_query_table.py'

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install pefile capstone
      - name: Decode NVAPI QueryInterface table
        run: |
          BIN=samples/work/unpacked/PhoenixMiner_6.2c_Windows/PhoenixMiner.exe.bin
          python scripts/decode_nvapi_query_table.py "$BIN" --out-dir notes
      - name: Commit result
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add notes/nvapi_query_table.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'analysis: decode PhoenixMiner NVAPI query table'
          git pull --rebase origin main
          git push
