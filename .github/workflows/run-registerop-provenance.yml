name: PM62C NVIDIA RegisterOp provenance
on:
  push:
    paths:
      - '.github/workflows/run-registerop-provenance.yml'
      - 'scripts/trace_registerop_function.py'
permissions:
  contents: write
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install pefile capstone
      - run: python scripts/trace_registerop_function.py samples/work/unpacked/PhoenixMiner_6.2c_Windows/PhoenixMiner.exe.bin --out-dir notes
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add notes/registerop_function_provenance.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'analysis: trace RegisterOp function provenance'
          git pull --rebase origin main
          git push
